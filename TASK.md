# Задача на рекомендацию следующего товара

## Обзор

Добро пожаловать! Это задание — часть отборочного процесса на траекторию рекомендательным системам и искусственному интеллекту.

Цель — построить простую рекомендательную систему, которая предсказывает следующий товар, с которым взаимодействует пользователь, на основе истории его просмотров. Предварительных знаний по машинному обучению не требуется — мы проверяем навыки Python, понимание теории вероятностей и умение рассуждать о данных и результатах.

## Данные

Вам предоставляется синтетический датасет e-commerce с пользовательскими сессиями просмотра товаров. Каждая сессия — это последовательность ID товаров, которые один пользователь просмотрел по порядку. Каждая сессия содержит не менее 3 товаров.

Данные хранятся в формате JSON Lines (`sessions.jsonl`): каждая строка — это JSON-массив, представляющий одну сессию. Элементы массива — целочисленные ID товаров:

```
[17, 42, 8, 103, 42]
[5, 99, 203, 17]
[203, 17, 88, 42, 7, 19]
[11, 11, 42]
...
```

Товары в сессии упорядочены по времени просмотра: первый элемент — самое раннее взаимодействие, последний — самое позднее.

Загрузить данные можно стандартными средствами Python:

```python
import json

sessions = []
with open("sessions.jsonl") as f:
    for line in f:
        line = line.strip()
        if line:
            sessions.append(json.loads(line))

# sessions — список списков целых чисел
print(f"Всего сессий: {len(sessions)}")
print(f"Первая сессия: {sessions[0]}")
```

## Задание

Постройте модель, которая по истории сессии пользователя предсказывает, с каким товаром он взаимодействует следующим.

Мы предлагаем следующий подход, но вы можете его адаптировать, если ответите на ключевые вопросы ниже.

**1. Анализ данных.** Посчитайте базовую статистику: число сессий, число уникальных товаров, распределение длин сессий, частоты товаров и любые другие сводные показатели, которые покажутся вам полезными. Сделайте несколько графиков. Отметьте всё, что покажется вам необычным или заслуживающим внимания.

**2. Train/test split.** Разбейте данные на обучающую и тестовую выборки по схеме, описанной ниже в разделе «Разбиение train / test». Будьте готовы объяснить, почему случайное разбиение не подходит для данных такого типа.

**3. Граф переходов.** На обучающих данных постройте граф, где вершины — товары, а рёбра — переходы между соседними товарами в рамках сессии. Посчитайте условную вероятность P(j | i) — вероятность того, что сразу после товара i будет просмотрен товар j. Продумайте, как обрабатывать товары или переходы, которые встречаются в обучении редко или не встречаются совсем.

**4. Рекомендательная модель.** По последнему товару в сессии возвращайте топ-10 кандидатов, отранжированных по вероятности перехода. Корректно обработайте случай, когда последний товар ни разу не встречался при обучении.

**5. Оценка качества.** Оцените модель на тестовой выборке с помощью метрики Hit@10 (определение и эталонная реализация ниже). Сравните результат с простым бейзлайном, который всегда рекомендует 10 самых популярных товаров. Если ваша модель не обходит бейзлайн — выясните, почему.

## Разбиение train / test

Для каждой сессии возьмите последний товар в качестве цели для предсказания, а всё, что до него, — в качестве истории сессии (обучающей части этой сессии).

Конкретно, для сессии `[a, b, c, d]`:

- Обучающая часть этой сессии: `[a, b, c]`
- Тестовая цель для этой сессии: `d`

Ваши обучающие данные — это набор всех таких укороченных сессий. Ваши тестовые цели — соответствующие последние товары, выровненные по индексу с обучающими сессиями.

### Эталонная реализация

```python
def train_test_split(sessions: list[list[int]]) -> tuple[list[list[int]], list[int]]:
    """
    Разбиение сессий на train и test.

    Для каждой сессии:
      - все товары кроме последнего становятся обучающей сессией
      - последний товар становится тестовой целью

    Возвращаемые списки выровнены по индексу: test_targets[i] — товар,
    который нужно предсказать по train_sessions[i] как истории.

    Parameters
    ----------
    sessions : list of lists of ints
        Каждый вложенный список — одна сессия ID товаров, упорядоченная по времени.
        Все сессии содержат не менее 3 товаров.

    Returns
    -------
    train_sessions : list of lists of ints
        Сессии для обучения (исходные без последнего товара).
    test_targets : list of ints
        Следующий товар для предсказания по каждой сессии.
    """
    train_sessions = [session[:-1] for session in sessions]
    test_targets = [session[-1] for session in sessions]
    return train_sessions, test_targets


# Использование
# train_sessions, test_targets = train_test_split(sessions)
# Для оценки: для каждого i предсказать следующий товар по train_sessions[i]
# и сравнить с test_targets[i].
```

Можете использовать эту функцию как есть или написать свою — главное, чтобы логика совпадала.

## Метрика оценки: Hit@10

Hit@10 измеряет, как часто истинный следующий товар попадает в топ-10 рекомендаций вашей модели.

Для одного тестового примера пусть `true_item` — товар, с которым пользователь действительно взаимодействовал следующим, а `recommendations` — список из 10 товаров, которые вернула модель. Попадание определяется так:

$$
\text{hit}_i = \begin{cases} 1 & \text{если true\_item} \in \text{recommendations} \\ 0 & \text{иначе} \end{cases}
$$

Hit@10 по всему тестовому набору — это среднее этих попаданий:

$$
\text{Hit@10} = \frac{1}{N} \sum_{i=1}^{N} \text{hit}_i
$$

где N — число тестовых примеров. Значение всегда от 0 до 1 — чем больше, тем лучше. Например, Hit@10 = 0.15 означает, что модель корректно помещает истинный следующий товар в свой топ-10 в 15% случаев.

Несколько замечаний:

- Позиция истинного товара внутри топ-10 не важна — важно только, попал он туда или нет.
- Возвращайте ровно 10 товаров на каждое предсказание. Если модель возвращает меньше (например, потому что для какого-то входа у неё меньше 10 кандидатов), это допустимо, но пустые места не могут стать попаданиями.
- Не включайте сам текущий товар в рекомендации, если на это нет особой причины.

### Эталонная реализация

```python
def hit_at_k(recommendations: list[list[int]], true_items: list[int], k: int = 10) -> float:
    """
    Вычисление Hit@K для списка предсказаний.

    Parameters
    ----------
    recommendations : list of lists of ints
        recommendations[i] — ранжированный список рекомендаций для i-го примера.
    true_items : list of ints
        true_items[i] — истинный следующий товар для i-го примера.
    k : int
        Отсечка top-K (по умолчанию 10).

    Returns
    -------
    float
        Hit@K, значение от 0 до 1.
    """
    assert len(recommendations) == len(true_items), \
        "recommendations и true_items должны совпадать по длине"

    hits = 0
    for recs, true_item in zip(recommendations, true_items):
        if true_item in recs[:k]:
            hits += 1

    return hits / len(true_items)
```

Можете использовать эту функцию как есть или написать свою — главное, чтобы результат совпадал.

## Что для нас важно

Мы не ищем идеальное решение. Мы ценим ясное мышление, чистый код и честные рассуждения о результатах. Простая модель, которую вы понимаете, лучше сложной, которую вы не понимаете.

В частности, мы ценим:

- Читаемый и воспроизводимый код
- Разумную обработку граничных случаев (короткие сессии, незнакомые товары, редкие переходы)
- Корректно реализованную оценку качества, которую вы можете обосновать
- Любопытство — умение заметить странность в данных или результатах и разобраться в ней — всегда хороший сигнал

## Технические требования

- Используйте Python. Стандартных библиотек достаточно; `collections`, `json` и `matplotlib` покроют большую часть нужд. NumPy разрешён, если вам так удобнее, но не обязателен.
- Не используйте готовые библиотеки рекомендательных систем (например, implicit, LightFM, RecBole). Граф переходов и рекомендательная модель должны быть вашей собственной реализацией.
- Присылайте решение в виде Jupyter-ноутбука или Python-скрипта.

## Отправка решения

Отправьте решение до **27.04.26** через канал отправки, предоставленный ВШЭ. Если что-то в задании непонятно — спрашивайте. Как вы задаёте вопросы — тоже часть сигнала для нас.

Удачи и приятной работы!
